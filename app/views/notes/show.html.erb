
  <div class="row">
    <div class="col-lg-12">
      <div class="note_show">
        <%= link_to "", notes_path, class: "fa fa-reply" %>
        <h1><%= @note.title %></h1>
        <p><%= simple_format(@note.content) %></p>
        <div class="show_note_options">
          <div class="note_options">
          <%= link_to "", edit_note_path(@note), class: "fa fa-pencil" %>
          <%= link_to "", note_path(@note), method: :delete, data: { confirm: "Etes-vous sûr ?"}, class: "fa fa-trash-o" %>
          </div>
        </div>
      </div>
    </div>
  </div>
